<UserControl x:Class="OmniKiosk.Wpf.Views.FaceRecognitionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:OmniKiosk.Wpf.Views"
             Background="Black">

    <UserControl.Resources>
        <Storyboard x:Key="ScannerAnimation">
            <DoubleAnimation Storyboard.TargetName="ScanningLine"
                             Storyboard.TargetProperty="(Canvas.Top)"
                             From="0" To="478" Duration="0:0:2"
                             AutoReverse="True" RepeatBehavior="Forever"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock x:Name="StatusText"
                       FontSize="26"
                       Foreground="White"
                       Margin="0,0,0,20"
                       TextAlignment="Center"
                       Text="Initializing..."/>

            <Grid x:Name="VideoContainer" Width="640" Height="480">
                <!-- This border is ONLY a frame. No Background! -->
                <Border x:Name="VideoBorder"
            BorderBrush="#2563EB"
            BorderThickness="3"
            CornerRadius="8"
            Background="{x:Null}" />

                <!-- Overlay layer (does NOT block native preview) -->
                <Canvas x:Name="CameraCanvas"
            Background="{x:Null}"
            IsHitTestVisible="False"
            Panel.ZIndex="10">

                    <Rectangle x:Name="FaceRect"
                   Stroke="#00FF00"
                   StrokeThickness="2"
                   Visibility="Collapsed"/>

                    <Rectangle x:Name="ScanningLine"
                   Width="634" Height="2"
                   Fill="#00FF00">
                        <Rectangle.Effect>
                            <DropShadowEffect Color="#00FF00" BlurRadius="10" ShadowDepth="0"/>
                        </Rectangle.Effect>
                    </Rectangle>
                </Canvas>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                <Button Content="Register Face" Click="RegisterCurrentFace_Click"
                        Width="180" Height="60" Margin="10"
                        Background="#10B981" Foreground="White"/>

                <Button Content="Back" Click="BtnBack_Click"
                        Width="180" Height="60" Margin="10"
                        Background="#EF4444" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
